# В процессе обучения пользования Git я прошёл обучение на Stepik, изучил несколько команд в терминале MacOs (н-р: nano, cd, mkdir), а также ознакомился с вспомогательной информацией в самой программе
# В ходе обучения возникли трудности с авторизацией на Github через терминал (пришлось использовать SSH ключ)
# Использованные команды Git: config, add, commit, log, status, diff, reflog, mv, rm, show, remote, push, branch, pull, fetch, revert,restore
# Для работы с удалённым репозиторием необходимо использовать команды remote, push, branch, pull
